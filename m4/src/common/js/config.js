/**
 * Created by sf on 2017/12/2.
 */

/**
 * 播放模式
 * @type {{sequence: number, loop: number, random: number}}
 */
export const playMode = {
    sequence: 0,  //顺序播放
    loop: 1,  //循环播放
    random: 2  //随机播放
}